---
title: "Iteration"
description: "A concise guide to Iteration."
slug: "8-1-4c-iteration"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="8-1-4c-iteration">Iteration</ParagraphTitle>

<ParagraphContent id="8-1-4c-iteration">

As stated in Chapter 7, there are three types of loop structures available to perform iterations so that a section of programming code can be repeated under certain conditions.

These are:

Count-controlled loops (for a set number of iterations)

### Pre-condition loops may have no iterations

Post-condition loops always has at least one iteration.

Find out about the loop structures available in the high-level programming language that your school has chosen to use.

### Count-controlled loops

```text
FOR loops are used when a set number of iterations are required. Look at some of
```

the different types of FOR statements with a counter starting at one, finishing at ten and increments by two for every iteration.

the table FOR loops

### For statement examples Language

```text
for Counter in range (1,11,2): Python uses the range function, a colon
```

to show the start of the for loop and print(Counter) indentation of all statements in the for loop.

```text
For Counter = 1 To 10 Step 2 Visual Basic uses Step and Next
```

### Console.WriteLine(Counter)

Java uses &#123;&#125; to show the start and end of the Next for loop.

```text
for (int Counter = 1; Counter &lt;= 10;
Counter = Counter + 2)
```

&#123;

System.out.println(Counter); &#125;

### Condition-controlled loops

When the number of iterations is not known, there are two options:

pre-condition loops which may have no iterations post-condition loops which always have at least one iteration.

Look at some of the different pre- and post-condition loops used in your programming language.

the table Pre-condition loops

### Pre-condition loops Language

```text
while TotalWeight &lt; 100: Python uses a colon to show the start of
```

the while loop and indentation to show TotalWeight = TotalWeight + Weight which statements are in the while loop.

### Visual Basic uses While and End While

While TotalWeight &lt; 100

```text
TotalWeight = TotalWeight + Weight Java uses &#123;&#125; to show the start and end of
```

the while loop.

### End While

```text
while (TotalWeight &lt; 100)
```

&#123;

```text
TotalWeight = TotalWeight + Weight;
```

&#125;

 the table Post-condition loops Language

### Python only

</ParagraphContent>
