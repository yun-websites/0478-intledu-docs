---
title: "Input and output"
description: "A concise guide to Input and output."
slug: "8-1-3-input-and-output"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="8-1-3-input-and-output">Input and output</ParagraphTitle>

<ParagraphContent id="8-1-3-input-and-output">

In order to be able to enter data and output results, programs need to use input and output statements. For IGCSE Computer Science you need to be able to write algorithms and programs that take input from a keyboard and output to a screen.

For a program to be useful, the user needs to know what they are expected to input, so each input needs to be accompanied by a prompt stating the input required.

Here are examples of inputs in programming code pseudocode was considered in Chapter 7. Just look at the section for the programming language you are using.

In a programming language the data type of the input must match the required data type of the variable where the input data is to be stored. All inputs default as strings, so if the input should be an integer or real number, commands are also used to change the data type of the input (for instance, in Python these are int() or float()).

the table Examples of input statements with prompts

### Find out more Input statements Language

```text
radius = float(input("Please enter the Python combines the prompt with the
```

These input radius of the cylinder ")) input statement. statements use Console.Write("Please enter the radius Visual Basic uses a separate prompt and a programming of the cylinder ") input. The input specifies the type of technique called data expected. casting, which radius = Decimal.Parse(Console. converts variables ReadLine()) In Java the input library has to be from one data type import java.util.Scanner; imported at the start of the program to another. Find and an input object is set up. Java uses

```text
out more about the Scanner myObj = new Scanner(System.in); a separate prompt and input. The input
```

use of casting in specifies the type and declares the the programming System.out.println("Please enter the variable of the same type. language that you radius of the cylinder "); have decided to use.

```text
double radius = myObj.nextDouble();
```

### Activity 8.2a

### In the high-level programming language

</ParagraphContent>
