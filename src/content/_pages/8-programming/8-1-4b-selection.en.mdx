---
title: "Selection"
description: "A concise guide to Selection."
slug: "8-1-4b-selection"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="8-1-4b-selection">Selection</ParagraphTitle>

<ParagraphContent id="8-1-4b-selection">

Selection is a very useful technique, allowing different routes through the steps of a program. For example, data items can be picked out according to given criteria, such as: selecting the largest value or smallest value, selecting items over a certain price, selecting everyone who is male.

Selection was demonstrated in pseudocode with the use of IF and CASE statements in Chapter 7.

Using the high-level programming language that your school has chosen, find out about the structure of IF...THEN...ELSE and CASE statements. Not all programming languages include the use of a CASE statement.

```text
IF statements
```

Look at some of the different types of IF statements available in your programming language.

the table IF statements single choice

```text
IF statement single choice example Language
IF Age &gt; 17 Pseudocode
```

```text
THEN
```

```text
OUTPUT "You are an adult"
```

```text
ENDIF Python does not use THEN or ENDIF
if Age &gt; 17: just a colon : and indentation
```

print ("You are an adult") Visual Basic uses Then and End If If Age &gt; 17 Then

Console.WriteLine("You are an adult")

End If Java uses curly brackets, &#123;, instead of If (Age &gt; 17) &#123; THEN and uses &#125; instead of ENDIF.

System.out.println ("You are an adult");

&#125;

the table IF statements single choice with alternative Language

### Pseudocode

```text
IF statement single choice with alternative example
IF Age &gt; 17
```

```text
THEN
OUTPUT "You are an adult"
```

```text
ELSE
OUTPUT "You are a child"
```

```text
ENDIF
```

the table Java uses else and curly brackets,

</ParagraphContent>
