---
title: "Declaring and populating arrays with iteration"
description: "A concise guide to Declaring and populating arrays with iteration."
slug: "8-2-2-declaring-and-populating-arrays-with-iteration"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="8-2-2-declaring-and-populating-arrays-with-iteration">Declaring and populating arrays with iteration</ParagraphTitle>

<ParagraphContent id="8-2-2-declaring-and-populating-arrays-with-iteration">

### One-dimensional arrays

A one-dimensional array can be referred to as a list. Here is an example of a list with 10 elements in it where the first element has an index of zero.

### First Index myList

element 0 27 1 19

### Last 2 36

element 3 42 4 16 5 89 6 21 7 16 8 55 9 72

When a one-dimensional array is declared in pseudocode:

the name of the array the first index value the last index value and the data type

are included. For example, to declare a new array called MyList:

```text
DECLARE MyList : ARRAY[0:9] OF INTEGER
```

Each position in the array can be populated in an array by defining the value at each index position. For instance, we can add the number 27 to the fourth position in the array MyList as follows:

MyList[4] 27

To populate the entire array instead we can use a loop:

```text
OUTPUT "Enter these 10 values in order 27, 19, 36, 42, 16, 89, 21, 16, 55, 72"
FOR Counter 0 TO 9
```

```text
OUTPUT "Enter next value "
INPUT MyList[Counter]
NEXT Counter
```

Notice that in this code we have used the variable Counter as the array index. We can display the data that lies in a particular location in an array as follows:

```text
OUTPUT MyList[1]
```

This would display the value 19.

### Activity 8.22

In your chosen programming language write a short program to declare and populate the array MyList, as shown in the diagram, using a FOR loop.

Arrays can also be populated as they are declared.

the table Array population Language

### Python

### Array Visual Basic

```text
myList = [27, 19, 36, 42, 16, 89, 21, 16, 55, 72] Java
Dim myList = New Integer() &#123;27, 19, 36, 42, 16, 89, 21, 16, 55, 72&#125;
int[] myList = &#123;27, 19, 36, 42, 16, 89, 21, 16, 55, 72&#125;;
```

### Two-dimensional arrays

A two-dimensional array can be referred to as a table, with rows and columns. Here is an example of a table with 10 rows and 3 columns, which contains

</ParagraphContent>
