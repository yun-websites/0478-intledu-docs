---
title: "Instruction set"
description: "A concise guide to Instruction set."
slug: "3-1-4-instruction-set"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

export const paragraphs = [
    {
        id: "3-1-4-instruction-set",
        title: "Instruction set",
        content: "In a computer system, instructions are a set of operations which are decoded in\n                       sequence. Each operation will instruct the ALU and CU (which are part of the\n                       CPU). An operation is made up of an opcode and an operand.\n\n                       The opcode informs the CPU what  The operand is the data which needs\n                       operation needs to be done       to be acted on or it can refer to a\n                                                        register in the memory\n\nLink                   Since the computer needs to understand the operation to be carried out, there\n                       is actually a limited number of opcodes that can be used; this is known as the\nSee Section 4.2.2 for  instruction set. All software running on a computer will contain a set of instructions\nexamples of opcodes    (which need to be converted into binary). The Fetch锟紻ecode锟紼xecute cycle is the\nand operands in        sequence of steps used by the CPU to process each instruction in sequence.\nassembly language.\n                       One example of an instruction set is the X86, a common CPU standard used in\nLink                   many modern computers. Although different computer manufacturers will adopt\n                       their own internal electronic design, if the computer is based on the X86 CPU\nFor more on            then all designs will share almost identical instruction sets. For example, Intel\ninterpreters and       Pentium and AMD Athlon CPUs use almost identical X86 instruction sets even\ncompilers see          though they are based on very different electronic designs.\nSection 4.2.3.\n                       (Note of caution: do not confuse instruction sets with programming code;\n                       instruction sets are the low-level language instructions that instruct the CPU\n                       how to carry out an operation. Program code needs interpreters or compilers\n                       to convert the code into the instruction set understood by the computer. Some\n                       examples of instruction set operations include: ADD, JMP, LDA, and so on.)\n\n82\n                                         3.1 Computer architecture",
    },
];

<ParagraphTitle id="3-1-4-instruction-set">Instruction set</ParagraphTitle>

<ParagraphContent id="3-1-4-instruction-set">{"In a computer system, instructions are a set of operations which are decoded in\n                       sequence. Each operation will instruct the ALU and CU (which are part of the\n                       CPU). An operation is made up of an opcode and an operand.\n\n                       The opcode informs the CPU what  The operand is the data which needs\n                       operation needs to be done       to be acted on or it can refer to a\n                                                        register in the memory\n\nLink                   Since the computer needs to understand the operation to be carried out, there\n                       is actually a limited number of opcodes that can be used; this is known as the\nSee Section 4.2.2 for  instruction set. All software running on a computer will contain a set of instructions\nexamples of opcodes    (which need to be converted into binary). The Fetch锟紻ecode锟紼xecute cycle is the\nand operands in        sequence of steps used by the CPU to process each instruction in sequence.\nassembly language.\n                       One example of an instruction set is the X86, a common CPU standard used in\nLink                   many modern computers. Although different computer manufacturers will adopt\n                       their own internal electronic design, if the computer is based on the X86 CPU\nFor more on            then all designs will share almost identical instruction sets. For example, Intel\ninterpreters and       Pentium and AMD Athlon CPUs use almost identical X86 instruction sets even\ncompilers see          though they are based on very different electronic designs.\nSection 4.2.3.\n                       (Note of caution: do not confuse instruction sets with programming code;\n                       instruction sets are the low-level language instructions that instruct the CPU\n                       how to carry out an operation. Program code needs interpreters or compilers\n                       to convert the code into the instruction set understood by the computer. Some\n                       examples of instruction set operations include: ADD, JMP, LDA, and so on.)\n\n82\n                                         3.1 Computer architecture"}</ParagraphContent>
