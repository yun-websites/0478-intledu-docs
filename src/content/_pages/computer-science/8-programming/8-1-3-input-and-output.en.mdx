---
title: "Input and output"
slug: "8-1-3-input-and-output"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

export const paragraphs = [
    {
        id: "8-1-3-input-and-output",
        title: "Input and output",
        content: "In order to be able to enter data and output results, programs need to use input\n                                and output statements. For IGCSE Computer Science you need to be able to write\n                                algorithms and programs that take input from a keyboard and output to a screen.\n\n                                For a program to be useful, the user needs to know what they are expected to input,\n                                so each input needs to be accompanied by a prompt stating the input required.\n\n                                Here are examples of inputs in programming code 锟� pseudocode was considered in\n                                Chapter 7. Just look at the section for the programming language you are using.\n\n304\n                                                                              8.1 Programming concepts\n\n                      In a programming language the data type of the input must match the required\n                      data type of the variable where the input data is to be stored. All inputs default\n                      as strings, so if the input should be an integer or real number, commands are\n                      also used to change the data type of the input (for instance, in Python these are\n                      int() or float()).\n\n                       Table 8.3 Examples of input statements with prompts\n\n      Find out more   Input statements                         Language\n                      radius = float(input(\"Please enter the   Python combines the prompt with the\nThese input           radius of the cylinder \"))               input statement.\nstatements use        Console.Write(\"Please enter the radius   Visual Basic uses a separate prompt and\na programming         of the cylinder \")                       input. The input specifies the type of\ntechnique called                                               data expected.\ncasting, which        radius = Decimal.Parse(Console.\nconverts variables    ReadLine())                              In Java the input library has to be\nfrom one data type    import java.util.Scanner;                imported at the start of the program\nto another. Find                                               and an input object is set up. Java uses\nout more about the    Scanner myObj = new Scanner(System.in);  a separate prompt and input. The input\nuse of casting in                                              specifies the type and declares the\nthe programming       System.out.println(\"Please enter the     variable of the same type.\nlanguage that you     radius of the cylinder \");\nhave decided to use.\n                      double radius = myObj.nextDouble();\n\n                      Activity 8.2a\n\n                      In the high-level programming language",
    },
];

<ParagraphTitle id="8-1-3-input-and-output">Input and output</ParagraphTitle>

<ParagraphContent id="8-1-3-input-and-output">{"In order to be able to enter data and output results, programs need to use input\n                                and output statements. For IGCSE Computer Science you need to be able to write\n                                algorithms and programs that take input from a keyboard and output to a screen.\n\n                                For a program to be useful, the user needs to know what they are expected to input,\n                                so each input needs to be accompanied by a prompt stating the input required.\n\n                                Here are examples of inputs in programming code 锟� pseudocode was considered in\n                                Chapter 7. Just look at the section for the programming language you are using.\n\n304\n                                                                              8.1 Programming concepts\n\n                      In a programming language the data type of the input must match the required\n                      data type of the variable where the input data is to be stored. All inputs default\n                      as strings, so if the input should be an integer or real number, commands are\n                      also used to change the data type of the input (for instance, in Python these are\n                      int() or float()).\n\n                       Table 8.3 Examples of input statements with prompts\n\n      Find out more   Input statements                         Language\n                      radius = float(input(\"Please enter the   Python combines the prompt with the\nThese input           radius of the cylinder \"))               input statement.\nstatements use        Console.Write(\"Please enter the radius   Visual Basic uses a separate prompt and\na programming         of the cylinder \")                       input. The input specifies the type of\ntechnique called                                               data expected.\ncasting, which        radius = Decimal.Parse(Console.\nconverts variables    ReadLine())                              In Java the input library has to be\nfrom one data type    import java.util.Scanner;                imported at the start of the program\nto another. Find                                               and an input object is set up. Java uses\nout more about the    Scanner myObj = new Scanner(System.in);  a separate prompt and input. The input\nuse of casting in                                              specifies the type and declares the\nthe programming       System.out.println(\"Please enter the     variable of the same type.\nlanguage that you     radius of the cylinder \");\nhave decided to use.\n                      double radius = myObj.nextDouble();\n\n                      Activity 8.2a\n\n                      In the high-level programming language"}</ParagraphContent>
