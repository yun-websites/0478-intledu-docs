---
title: "Validation"
description: "A concise guide to Validation."
slug: "7-5-1-validation"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="7-5-1-validation">Validation</ParagraphTitle>

<ParagraphContent id="7-5-1-validation">

Validation is the automated checking by a program that data is reasonable before it is accepted into a computer system. When data is validated by a computer system, if the data is rejected a message should be output explaining why the data was rejected and another opportunity given to enter the data.

There are many different types of validation checks including:

range checks length checks type checks presence checks format checks check digits.

### Password has a 7.5 Validation and verification

data type of string and LENGTH is Different types of check may be used on the same piece of data; for example, an the pseudocode examination mark could be checked for reasonableness by using a range check, a operation that type check and a presence check. returns a whole Range check number showing A range check checks that the value of a number is between an upper value and the number of a lower value. For example, checking that percentage marks are between 0 and characters in the 100 inclusive: string

```text
OUTPUT "Please enter the student's mark "
REPEAT
INPUT StudentMark
IF StudentMark &lt; 0 OR StudentMark &gt; 100
```

```text
THEN
OUTPUT "The student's mark should be in the range
```

0 to 100, please re-enter the mark "

```text
ENDIF
UNTIL StudentMark &gt;= 0 AND StudentMark &lt;= 100
```

### Length check

A length check checks either: that data contains an exact number of characters, for example that a password

must be exactly eight characters in length so that passwords with seven or fewer characters or nine or more characters would be rejected, for instance:

```text
OUTPUT "Please enter your password of eight
```

characters "

```text
REPEAT
```

```text
INPUT Password
IF LENGTH(Password) &lt;&gt; 8
```

```text
THEN
OUTPUT "Your password must be exactly eight
```

c haracters, please re-enter "

</ParagraphContent>
