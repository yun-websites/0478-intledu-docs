---
title: "Linear search"
description: "A concise guide to Linear search."
slug: "7-4-4-linear-search"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="7-4-4-linear-search">Linear search</ParagraphTitle>

<ParagraphContent id="7-4-4-linear-search">

A search is used to check if a value is stored in a list, performed by systematically working through the items in the list. There are several standard search methods, but you only need to understand one method for IGCSE Computer Science. This is called a linear search, which inspects each item in a list in turn to see if the item matches the value searched for.

For example, searching for a name in a class list of student names, where all the names stored are different:

```text
OUTPUT "Please enter name to find " Setting a variable,
INPUT Name Found, as a flag, using
```

### Found FALSE TRUE and FALSE to

indicate if the name has been found or not

### Counter 1

```text
REPEAT Checking if the name
IF Name = StudentName[Counter] input matches a name
```

in the list

```text
THEN Setting the flag to
```

### Found TRUE TRUE when a match

is found

```text
ELSE
```

Counter Counter + 1 Stopping the search when a match is

```text
ENDIF found or the whole
UNTIL Found OR Counter &gt; ClassSize list has been searched
IF Found
```

### Outputting the

position in the list THEN when a match is OUTPUT Name, " found at position ", found Counter, " in the list."

```text
ELSE
OUTPUT Name, " not found."
```

```text
ENDIF
```

In this example, the search checks how many people chose ice cream as their favourite dessert, where several values in the list can be the same.

### ChoiceCount 0 Checking ice cream

```text
FOR Counter 1 TO Length has been chosen
IF "ice cr
```

</ParagraphContent>
