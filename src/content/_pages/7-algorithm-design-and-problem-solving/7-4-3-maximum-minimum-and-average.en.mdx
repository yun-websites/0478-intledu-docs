---
title: "Maximum, minimum and average"
description: "A concise guide to Maximum, minimum and average."
slug: "7-4-3-maximum-minimum-and-average"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="7-4-3-maximum-minimum-and-average">Maximum, minimum and average</ParagraphTitle>

<ParagraphContent id="7-4-3-maximum-minimum-and-average">

Finding the largest and smallest values in a list are two standard methods that are frequently found in algorithms, for example, finding the highest and lowest mark awarded to a class of students.

### Initialising MaximumMark 0 Initialising

maximum to the MinimumMark 100 minimum to the lowest mark FOR Counter 1 TO ClassSize highest possible possible

```text
IF StudentMark[Counter] &gt; MaximumMark Replacing the
```

### Calls data from an THEN maximum mark

array (see Chapter 8) MaximumMark StudentMark[Counter] with a higher mark called StudentMark)

```text
ENDIF Replacing the
IF StudentMark[Counter] &lt; MinimumMark minimum mark
```

with a lower mark

```text
THEN
```

MinimumMark StudentMark[Counter]

```text
ENDIF
NEXT Counter
```

7 Algorithm design and problem solving

If the largest and smallest values are not known, an alternative method is to set the maximum and minimum values to the first item in the list.

For example, using this method to find the highest and lowest mark awarded to a

```text
class of students.
```

MaximumMark StudentMark[1] Initialising minimum MinimumMark StudentMark[1] and maximum to the

```text
FOR Counter 2 TO ClassSize first mark
```

Starting the loop at IF StudentMark[Counter] &gt; MaximumMark the second position in THEN the list. MaximumMark StudentMark[Counter]

```text
ENDIF
IF StudentMark[Counter] &lt; MinimumMark
```

```text
THEN
```

MinimumMark StudentMark[Counter]

```text
ENDIF
NEXT Counter
```

Calculating the average (mean) of all the values in a list is an extension of the totalling method, for example, calculating the average mark for a class of students.

### Total 0 Calculating the

```text
FOR Counter 1 TO ClassSize average from the total
```

</ParagraphContent>
