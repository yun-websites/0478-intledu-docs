---
title: "Symmetric and asymmetric encryption"
description: "A concise guide to Symmetric and asymmetric encryption."
slug: "2-3-2-symmetric-and-asymmetric-encryption"
chapter: "computer-science"
version: "2025"
updateAt: "2025-01-13"
---

<ParagraphTitle id="2-3-2-symmetric-and-asymmetric-encryption">Symmetric and asymmetric encryption</ParagraphTitle>

<ParagraphContent id="2-3-2-symmetric-and-asymmetric-encryption">

### Symmetric encryption

Symmetric encryption uses an encryption key; the same key is used to encrypt and decrypt the encoded message. First of all, consider a simple system that uses a 10-digit denary encryption key (this gives 1 1010 possible codes); and a decryption key. Suppose our encryption key is:

4291362856

which means every letter in a word is shifted across the alphabet +4, +2, +9, +1, and so on, places. For example, here is the message COMPUTER SCIENCE IS EXCITING (plaintext on the top line of the diagram) before and after applying the encryption key (forming the ciphertext shown on the bottom line of the diagram):

COMPU T E R SC I ENCE IS E XC I T I NG 42913628 5642913 62 85642913 GQ V Q X ZGZ X I MGWDH OU MC I MV RO J

Find out more To get back to the original message, it will be necessary to apply the same decryption key; that is, 4 2 9 1 3 6 2 8 5 6. But in this case, the decryption One of the ways of process would be the reverse of encryption and each letter would be shifted 4, mitigating the risk of 2, 9, 1, and so on. For example, `G' `C', `Q' `O', `V' `M', `Q' `P', and symmetric keys falling so on. into the wrong hands (known as the key However, modern computers could `crack' this encryption key in a matter of distribution problem) seconds. To try to combat this, we now use 256-bit binary encryption keys that is to use a system give 2256 (approximately, 1.2 1077) possible combinations. (Even this may not be based on modulo-11, enough as we head towards quantum computers.) where both sender and receiver can calculate The real difficulty is keeping the encryption key a secret (for example, it needs the encryption key to be sent in an email or a text message which can be intercepted). Therefore, without it actually the issue of security is always the main drawback of symmetrical encryption, being exchanged in any since a single encryption key is required for both se

</ParagraphContent>
